import React from 'react';
import EditListResult from './EditListResult';

const EditListSection = () => {
  return (
    <section id="library-view" className="library-view">
      <EditListResult />
    </section>
  );
};

export default EditListSection;
